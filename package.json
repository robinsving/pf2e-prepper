{
    "name": "pf2e-prepper",
    "version": "1.0.0",
    "description": "",
    "license": "MIT",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "commitlint": "commitlint --config commitlint.config.js --from-last-tag",
        "release": "node scripts/create-release.mjs",
        "publish-dry": "node scripts/publish-release.mjs --dry-run",
        "publish": "node scripts/publish-release.mjs",
        "test": "vitest run"
    },
    "devDependencies": {
        "@commitlint/cli": "^20.4.2",
        "@commitlint/config-conventional": "^20.4.2",
        "rollup-plugin-copy": "^3.5.0",
        "vite": "^7.3.1",
        "vite-plugin-singlefile": "^2.3.0",
        "vitest": "^4.0.18"
    }
}
